---
layout: "default.njk"
hide_page_title: true
---
<article class="post">
    <header class="post-header">
        <h1 class="post-title">{{ title }}</h1>
        <time class="post-date" datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time>
    </header>

    {# --- SOLUZIONE --- #}
    {# Accediamo direttamente a "featured_image" #}
    {% if featured_image %}
        <img src="{{ featured_image | url }}" alt="Immagine di anteprima per {{ title }}" class="post-featured-image">
    {% endif %}

    <div class="post-content">
        {{ content | safe }}
    </div>

{# ======================================================== #}
    {# Inizio: Sostituisci la vecchia firma e CTA con questo blocco #}
    {# ======================================================== #}
    <footer class="author-block">
        <div class="author-image">
            <img src="{{ '/img/profilopierg.JPG' | url }}" alt="Foto di Piergiorgio Grossi">
        </div>
        <div class="author-info">
            <span class="author-label">AUTORE</span>
            <h4 class="author-name">Piergiorgio Grossi</h4>
            <p class="author-bio">
                My first ChatGPT was a printed manual:<br><strong>building what's next.</strong>
            </p>            
            
            {% if linkedin_url %}
            <div class="author-linkedin">
                <a href="{{ linkedin_url }}" class="linkedin-button" target="_blank" rel="noopener noreferrer">Leggi il post su LinkedIn</a>
            </div>
            {% endif %}
        </div>
    </footer>
    {# ======================================================== #}
    {# Fine: Blocco di sostituzione                             #}
    {# ======================================================== #}
</article>