---
layout: "default.njk"
---
<article class="post">

    {# L'immagine in evidenza ora è la prima cosa dentro l'articolo #}
    {% if featured_image %}
        <img src="{{ featured_image | url }}" alt="Immagine di anteprima per {{ title }}" class="post-featured-image">
    {% endif %}

    <div class="post-content">
        {{ content | safe }}
    </div>

    {# Il blocco autore rimane invariato #}
    <footer class="author-block">
        <div class="author-image">
            <img src="{{ '/img/profilopierg.JPG' | url }}" alt="Foto di Piergiorgio Grossi">
        </div>
        <div class="author-info">
            <span class="author-label">AUTORE</span>
            <h4 class="author-name">Piergiorgio Grossi</h4>
            <p class="author-bio">
                My first ChatGPT was a printed manual:<br><strong>building what's next.</strong>
            </p>
            {% if linkedin_url %}
            <div class="author-linkedin">
                <a href="{{ linkedin_url }}" class="linkedin-button" target="_blank" rel="noopener noreferrer">Leggi l'originale su LinkedIn</a>
            </div>
            {% endif %}
        </div>
    </footer>

</article>